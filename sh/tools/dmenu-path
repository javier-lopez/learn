#!/bin/sh
CACHE=$HOME/.dmenu_cache
IFS=:

uptodate()
{
    [ -f "$CACHE" ] &&
        for dir in $PATH
        do
            [ ! $dir -nt "$CACHE" ] || return 1
        done
}

if ! uptodate ; then
    for dir in $PATH
    do
        cd "$dir" &&
            for file in *
            do
                [ -e "$file" ] && printf "%s\\n" "$file"
            done
        done | sort | uniq > "$CACHE".$$ &&
            mv "$CACHE".$$ "$CACHE"
fi

cat "$CACHE"
