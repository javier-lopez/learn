#!/usr/bin/env bash
#helps to edit custom scripts

program="$1"

_usage()
{
    echo "Usage: $(expr "$0" : '.*/\([^/]*\)') foo_program"
    exit 0
}

[ -n "$program" ] || _usage

if command -v "$program" 2>/dev/null; then
    $EDITOR $(command -v "$program" 2>/dev/null)
fi
